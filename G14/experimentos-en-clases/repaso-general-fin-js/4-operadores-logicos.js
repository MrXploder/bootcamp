let operadorY = true && true;
let operadorO = true || true;
let negacion = !true;

let lamparaRota;

if (lamparaRota === "si" || lamparaRota === "tal vez") {
  //cambiar ampolleta
} else {
  //dejarla como estaba
}
//          false         ||           false
if (lamparaRota === "no" || lamparaRota === "talvez") {
  //cambiarla de todas formas
}

